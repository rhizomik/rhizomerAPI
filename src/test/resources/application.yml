allowed-origins: http://localhost:4200

spring:
  profiles:
    active: LOCAL_SERVER_TESTING
  jpa:
    hibernate:
      ddl-auto: update

rhizomer:
  default-password: password
  sparql-timeout: 120000
  omit:
    properties:
    classes:
