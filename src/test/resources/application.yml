allowed-origins: https://rhizomer.rhizomik.net, http://localhost:4200

spring:
  profiles:
    active: LOCAL_SERVER_TESTING

rhizomer:
  default-password: password
  omit:
    properties:
    classes:
